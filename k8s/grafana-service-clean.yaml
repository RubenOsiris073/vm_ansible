apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: kube-system
  labels:
    app: grafana
spec:
  type: NodePort
  ports:
  - name: https
    port: 3000
    targetPort: 3000
    nodePort: 30093
    protocol: TCP
  selector:
    app: grafana